<section class="products">
    <ul> {% for item in products %}
        <li><a href="{{ item.href }}"><img src="{{item.src}}" alt="{{item.alt}}"></a>{{item.name}}</li>
    {% endfor %}
    </ul>
</section>